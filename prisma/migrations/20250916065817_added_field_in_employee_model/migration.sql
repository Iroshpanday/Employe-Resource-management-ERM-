BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Employee] ADD [address] NVARCHAR(1000),
[gender] NVARCHAR(1000),
[status] NVARCHAR(1000) NOT NULL CONSTRAINT [Employee_status_df] DEFAULT 'Active';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
